{
  "name": "wetter-tagesplan-app",
  "version": "1.3.0",
  "description": "Wetter & Tagesplan – Wetter-App mit Open-Meteo API, Favoriten, Cache und personalisierten Tagesempfehlungen",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write \"**/*.{js,css,html,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,css,html,json,md}\""
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "prettier": "^3.3.3",
    "vitest": "^2.0.5",
    "jsdom": "^25.0.0"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0"
  }
}
